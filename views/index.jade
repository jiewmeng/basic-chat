extends layout

block content
	h1= title
	p Welcome to #{title}

	p 
		if !!auth.user
			| Logged in as #{auth.user.name} 
			a(href="/chatroom") Enter chat
			| 
			a(href="/auth/logout") Logout

		else
			a(href="/auth/facebook") Login with Facebook
